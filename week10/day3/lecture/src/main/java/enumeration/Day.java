package enumeration;

public enum Day {
    // take note that enums are both constant and static
    // we do not construct instances of nums in our code
    SUNDAY, MONDAY, TUESDAY, WEDNESDAY,
    THURSDAY, FRIDAY, SATURDAY
}
